<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>비밀번호 찾기</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .container { max-width: 400px; margin-top: 100px; }
        .find-links a { text-decoration: none; color: #6c757d; } /* 링크 밑줄 제거 및 색상 변경 */
        .find-links a:hover { color: #212529; } /* 마우스 올렸을 때 색상 변경 */
        .divider { margin: 0 10px; color: #ced4da; } /* 구분선 스타일 */
    </style>
</head>
<body>

<div class="container">
    <h2 class="text-center mb-4">비밀번호 찾기</h2>

    {{#errorMessage}}
        <div class="alert alert-danger">{{errorMessage}}</div>
    {{/errorMessage}}

    {{^isUserVerified}}
        <p class="text-muted text-center mb-3">회원가입 시 등록한 아이디와 이름, 전화번호 정보를 입력해주세요.</p>
        <form action="/user/verify-account" method="post">
            <div class="mb-3">
                <input type="text" name="loginId" class="form-control" placeholder="아이디" required>
            </div>
            <div class="mb-3">
                <input type="text" name="realName" class="form-control" placeholder="이름" required>
            </div>
            <div class="mb-3">
                <input type="tel" name="phone" class="form-control" placeholder="전화번호" required>
            </div>
            <button type="submit" class="btn btn-primary w-100 mb-3">계정 정보 확인</button>
        </form>
    {{/isUserVerified}}


    {{#isUserVerified}}
        <p class="text-muted text-center mb-3">새로운 비밀번호를 입력해주세요.</p>
        <form action="/user/reset-password" method="post" onsubmit="return validatePassword();">
            <input type="hidden" name="loginId" value="{{user.loginId}}">

            <div class="mb-3">
                <input type="password" id="newPassword" name="newPassword" class="form-control" placeholder="새 비밀번호" required>
            </div>
            <div class="mb-3">
                <input type="password" id="confirmPassword" name="confirmPassword" class="form-control" placeholder="새 비밀번호 확인" required>
            </div>
            <button type="submit" class="btn btn-success w-100 mb-3">비밀번호 변경하기</button>
        </form>
    {{/isUserVerified}}

    <form action="/user/login">
    <div class="mt-3 d-flex justify-content-center find-links">
        <button type="submit" class="btn btn-success w-100 mb-3">로그인 페이지로 이동</button>
    </div>
    </form>
</div>

<script>
    // 간단한 클라이언트 측 비밀번호 일치 검사
    function validatePassword() {
        const newPassword = document.getElementById('newPassword').value;
        const confirmPassword = document.getElementById('confirmPassword').value;

        if (newPassword !== confirmPassword) {
            alert("새 비밀번호와 비밀번호 확인이 일치하지 않습니다.");
            return false; // 폼 제출(submit)을 막음
        }
        return true; // 폼 제출을 진행
    }
</script>

</body>
</html>