<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>로그인</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .container { max-width: 400px; margin-top: 100px; }
        .find-links a { text-decoration: none; color: #6c757d; } /* 링크 밑줄 제거 및 색상 변경 */
        .find-links a:hover { color: #212529; } /* 마우스 올렸을 때 색상 변경 */
        .divider { margin: 0 10px; color: #ced4da; } /* 구분선 스타일 */
    </style>
</head>
<body>

<div class="container text-center">
    <h2>로그인</h2>

    {{#errorMessage}}
        <div class="alert alert-danger" role="alert">
            {{errorMessage}}
        </div>
    {{/errorMessage}}

    {{#successMessage}}
        <div class="alert alert-success" role="alert">
            {{successMessage}}
        </div>
    {{/successMessage}}

    <form action="/user/login" method="post">
        <div class="mb-3">
            <input type="text" name="username" class="form-control" placeholder="아이디" required>
        </div>
        <div class="mb-3">
            <input type="password" name="password" class="form-control" placeholder="비밀번호" required>
        </div>
        <button type="submit" class="btn btn-primary w-100 mb-3">로그인</button>
    </form>
    <a href="/user/register" class="btn btn-secondary w-100">회원가입</a>

    <div class="mt-3 d-flex justify-content-center find-links">
        <a href="/user/find_id">아이디 찾기</a>
        <span class="divider">|</span>
        <a href="/user/find_pw">비밀번호 찾기</a>
    </div>

</div>

</body>
</html>