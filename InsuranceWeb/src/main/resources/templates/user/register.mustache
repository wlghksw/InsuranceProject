<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>회원가입</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <style>
        body {
            padding-top: 80px;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }
        
        /* 네비바 스타일 */
        .custom-navbar {
            background-color: #1c2f48;
            border-bottom: 1px solid rgba(0,0,0,.25);
            padding: 8px 20px;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }
        .custom-navbar .navbar-brand {
            color: #d4b46b !important;
            font-weight: 900;
            font-size: 40px;
            margin-right: 30px;
        }
        .custom-navbar .navbar-nav {
            margin-left: auto;
        }
        
        /* 네비바 버튼 스타일 */
        .nav-btn {
            background-color: transparent;
            border: 2px solid #d4b46b;
            color: #d4b46b !important;
            padding: 8px 20px;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            margin: 0 5px;
        }
        .nav-btn:hover {
            background-color: #d4b46b;
            color: #1c2f48 !important;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(212, 180, 107, 0.3);
        }
        .nav-btn.active {
            background-color: #d4b46b;
            color: #1c2f48 !important;
        }
        .nav-btn-kakao {
            background-color: #FEE500;
            border: 2px solid #FEE500;
            color: #3c1e1e !important;
        }
        .nav-btn-kakao:hover {
            background-color: #fdd835;
            border-color: #fdd835;
            color: #3c1e1e !important;
        }
        .user-welcome {
            color: #d4b46b;
            font-weight: 600;
            padding: 8px 15px;
            margin-right: 10px;
        }
        
        /* 회원가입 컨테이너 */
        .register-container { 
            max-width: 550px; 
            margin: 50px auto;
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.1);
        }
        .register-container h2 {
            color: #1c2f48;
            font-weight: 700;
            margin-bottom: 30px;
        }
        .btn-primary {
            background-color: #1c2f48;
            border: none;
            padding: 12px;
            font-weight: 600;
        }
        .btn-primary:hover {
            background-color: #2a4a6f;
        }
        .btn-secondary {
            background-color: #6c757d;
            border: none;
            padding: 12px;
            font-weight: 600;
        }
        .form-control {
            padding: 12px;
            border-radius: 10px;
        }
        .form-label {
            font-weight: 600;
            color: #333;
        }
    </style>
</head>
<body>

<!-- 네비게이션 바 -->
<nav class="custom-navbar navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="/main">B</a>
        <ul class="navbar-nav">

            {{#name}}
                <!-- 로그인 상태 -->
                <li class="nav-item">
                    <span class="user-welcome">
                        <i class="fas fa-user-circle me-2"></i>{{name}}님
                    </span>
                </li>
                <li class="nav-item">
                    <a class="nav-btn" href="/user/myPage">
                        <i class="fas fa-user me-2"></i>마이페이지
                    </a>
                </li>
                <li class="nav-item">
                    <form method="post" action="/logout" class="d-inline">
                        <button type="submit" class="nav-btn">
                            <i class="fas fa-sign-out-alt me-2"></i>로그아웃
                        </button>
                    </form>
                </li>
            {{/name}}
        </ul>
    </div>
</nav>

<div class="register-container">
    <h2 class="text-center"><i class="fas fa-user-plus me-2"></i>회원가입</h2>

    {{#errorMessage}}
        <div class="alert alert-danger" role="alert">
            {{errorMessage}}
        </div>
    {{/errorMessage}}

    <form action="/user/register" method="post">
        <div class="mb-3">
            <label for="loginId" class="form-label">아이디</label>
            <input type="text" class="form-control" id="loginId" name="loginId" required>
        </div>
        <div class="mb-3">
            <label for="password" class="form-label">비밀번호</label>
            <input type="password" class="form-control" id="password" name="password" required>
        </div>
        <div class="mb-3">
            <label for="nickname" class="form-label">닉네임</label>
            <input type="text" class="form-control" id="nickname" name="nickname" required>
        </div>
        <div class="mb-3">
            <label for="realName" class="form-label">이름</label>
            <input type="text" class="form-control" id="realName" name="realName" required>
        </div>
        <div class="mb-3">
            <label for="phone" class="form-label">휴대폰 번호(010-0000-0000)</label>
            <input type="tel" class="form-control" id="phone" name="phone" required>
        </div>
        <div class="mb-3">
            <label for="birthYear" class="form-label">생년월일(YYYYMMDD)</label>
            <input type="text" class="form-control" id="birthYear" name="birthYear" placeholder="예: 19901026" required>
        </div>
        <div class="mb-3">
            <label for="gender" class="form-label">성별</label>
            <select class="form-select" id="gender" name="gender" required>
                <option value="male">남성</option>
                <option value="female">여성</option>
            </select>
        </div>
        <button type="submit" class="btn btn-success w-100">회원가입</button>
    </form>
</div>

</body>
</html>