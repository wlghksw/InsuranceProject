<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>로그인</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <style>
        body {
            padding-top: 80px;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }
        
        /* 네비바 스타일 */
        .custom-navbar {
            background-color: #1c2f48;
            border-bottom: 1px solid rgba(0,0,0,.25);
            padding: 8px 20px;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }
        .custom-navbar .navbar-brand {
            color: #d4b46b !important;
            font-weight: 900;
            font-size: 40px;
            margin-right: 30px;
        }
        .custom-navbar .navbar-nav {
            margin-left: auto;
        }
        
        /* 네비바 버튼 스타일 */
        .nav-btn {
            background-color: transparent;
            border: 2px solid #d4b46b;
            color: #d4b46b !important;
            padding: 8px 20px;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            margin: 0 5px;
        }
        .nav-btn:hover {
            background-color: #d4b46b;
            color: #1c2f48 !important;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(212, 180, 107, 0.3);
        }
        .nav-btn.active {
            background-color: #d4b46b;
            color: #1c2f48 !important;
        }
        .nav-btn-kakao {
            background-color: #FEE500;
            border: 2px solid #FEE500;
            color: #3c1e1e !important;
        }
        .nav-btn-kakao:hover {
            background-color: #fdd835;
            border-color: #fdd835;
            color: #3c1e1e !important;
        }
        .user-welcome {
            color: #d4b46b;
            font-weight: 600;
            padding: 8px 15px;
            margin-right: 10px;
        }
        
        /* 로그인 컨테이너 */
        .login-container { 
            max-width: 450px; 
            margin: 50px auto;
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.1);
        }
        .login-container h2 {
            color: #1c2f48;
            font-weight: 700;
            margin-bottom: 30px;
        }
        .btn-primary {
            background-color: #1c2f48;
            border: none;
            padding: 12px;
            font-weight: 600;
        }
        .btn-primary:hover {
            background-color: #2a4a6f;
        }
        .btn-secondary {
            background-color: #d4b46b;
            border: none;
            color: #1c2f48;
            padding: 12px;
            font-weight: 600;
        }
        .btn-secondary:hover {
            background-color: #c5a55b;
            color: #1c2f48;
        }
        .form-control {
            padding: 12px;
            border-radius: 10px;
        }
    </style>
</head>
<body>

<!-- 네비게이션 바 -->
<nav class="custom-navbar navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="/main">거북이 보험</a>
        <ul class="navbar-nav">

        </ul>
    </div>
</nav>


<div class="login-container text-center">
    <h2><i class="fas fa-lock me-2"></i>로그인</h2>

    {{#errorMessage}}
        <div class="alert alert-danger" role="alert">
            {{errorMessage}}
        </div>
    {{/errorMessage}}

    {{#successMessage}}
        <div class="alert alert-success" role="alert">
            {{successMessage}}
        </div>
    {{/successMessage}}

    <form action="/user/login" method="post">
        <div class="mb-3">
            <input type="text" name="username" class="form-control" placeholder="아이디" required>
        </div>
        <div class="mb-3">
            <input type="password" name="password" class="form-control" placeholder="비밀번호" required>
        </div>
        <button type="submit" class="btn btn-primary w-100 mb-3">로그인</button>
    </form>
    
    <!-- 카카오 로그인 버튼 -->
    <a href="/oauth2/authorization/kakao" class="btn w-100 mb-3" style="background-color: #FEE500; color: #3c1e1e; border: none; font-weight: 600;">
        <i class="fas fa-comment me-2"></i>카카오 계정으로 로그인
    </a>
    
    <div class="d-flex justify-content-between align-items-center mb-3">
        <hr style="flex: 1; border-top: 1px solid #dee2e6;">
        <span class="px-3 text-muted" style="font-size: 0.9rem;">또는</span>
        <hr style="flex: 1; border-top: 1px solid #dee2e6;">
    </div>
    
    <a href="/user/register" class="btn btn-secondary w-100 mb-3">회원가입</a>
    <div class="mt-2">
        <a href="/user/find_id" style="color: #6c757d; text-decoration: none;">아이디 찾기</a>
    </div>
</div>

</body>
</html>