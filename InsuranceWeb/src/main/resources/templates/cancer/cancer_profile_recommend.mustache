<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
    <title>특성기반 암보험 추천 - 거북이 보험</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            background-color: #f8f9fa;
            padding-top: 80px;
        }
        .custom-navbar {
            background-color: #fcf8f0;
            border-bottom: 1px solid #e0e0e0;
            padding: 1rem 1.5rem;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }
        .custom-navbar .navbar-brand {
            font-family: 'Noto Sans KR', sans-serif;
            font-weight: 700;
            color: #17ec42;
            font-size: 1.8rem;
        }
        .custom-navbar .nav-link {
            font-weight: 700;
            color: #555;
            margin-right: 1.5rem;
        }
        .custom-navbar .nav-link:hover {
            color: #17ec42;
        }
        .recommend-form {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            padding: 2rem;
            margin-bottom: 2rem;
        }
        .form-label {
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
        }
        .form-control, .form-select {
            border-radius: 10px;
            border: 2px solid #e9ecef;
            padding: 0.75rem 1rem;
        }
        .form-control:focus, .form-select:focus {
            border-color: #17ec42;
            box-shadow: 0 0 0 0.2rem rgba(23, 236, 66, 0.25);
        }
        .btn-recommend {
            background-color: #17ec42;
            border: none;
            border-radius: 25px;
            padding: 0.75rem 2rem;
            font-weight: 600;
            font-size: 1.1rem;
        }
        .btn-recommend:hover {
            background-color: #15d65a;
            transform: translateY(-2px);
        }
        .result-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            padding: 1.5rem;
            margin-bottom: 1rem;
            transition: transform 0.2s ease;
        }
        .result-card:hover {
            transform: translateY(-5px);
        }
        .company-name {
            color: #17ec42;
            font-weight: 700;
            font-size: 1.2rem;
        }
        .product-name {
            color: #333;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        .coverage-amount {
            color: #dc3545;
            font-weight: 700;
            font-size: 1.3rem;
        }
        .premium-info {
            color: #666;
            font-size: 0.9rem;
        }
        .score-badge {
            background-color: #17ec42;
            color: white;
            padding: 0.25rem 0.75rem;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        .loading {
            display: none;
            text-align: center;
            padding: 2rem;
        }
        .spinner-border {
            color: #17ec42;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg custom-navbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="/main">🐢거북이 보험</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/service_intro">서비스 소개</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/user/my_insurance">내 보험찾기</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/notice">공지사항</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                {{#user}}
                    <li class="nav-item">
                        <span class="nav-link text-dark">안녕하세요. {{nickname}}님</span>
                    </li>
                    <li class="nav-item">
                        <form action="/logout" method="post" class="d-inline">
                            <button type="submit" class="btn btn-outline-dark">로그아웃</button>
                        </form>
                    </li>
                {{/user}}
                {{^user}}
                    <li class="nav-item">
                        <a class="nav-link text-dark" href="/user/login">로그인</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" href="/user/register">회원가입</a>
                    </li>
                {{/user}}
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="text-center mb-4">
                <h1 style="color: #17ec42; font-weight: bold;">맞춤형 암보험 추천</h1>
                <p class="text-muted">나이, 성별, 직업, 예산 등 당신의 특성만으로 최적의 암보험을 추천받으세요</p>
            </div>

            <div class="recommend-form">
                <form id="profileForm">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="age" class="form-label">나이</label>
                            <input type="number" class="form-control" id="age" name="age" 
                                   placeholder="예: 35" min="18" max="80" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="sex" class="form-label">성별</label>
                            <select class="form-select" id="sex" name="sex" required>
                                <option value="">성별을 선택하세요</option>
                                <option value="M">남성</option>
                                <option value="F">여성</option>
                            </select>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="smokerFlag" class="form-label">흡연 여부</label>
                            <select class="form-select" id="smokerFlag" name="smokerFlag" required>
                                <option value="0">비흡연</option>
                                <option value="1">흡연</option>
                            </select>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="monthlyBudget" class="form-label">월 예산 (원)</label>
                            <input type="number" class="form-control" id="monthlyBudget" name="monthlyBudget" 
                                   placeholder="예: 50000" min="10000" max="500000" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="topN" class="form-label">추천 상품 개수</label>
                            <select class="form-select" id="topN" name="topN">
                                <option value="5">5개</option>
                                <option value="10" selected>10개</option>
                                <option value="15">15개</option>
                                <option value="20">20개</option>
                            </select>
                        </div>
                    </div>
                    
                    <div class="text-center">
                        <button type="submit" class="btn btn-recommend">
                            <i class="fas fa-search me-2"></i>보험 추천받기
                        </button>
                    </div>
                </form>
            </div>

            <div class="loading" id="loading">
                <div class="spinner-border" role="status">
                    <span class="visually-hidden">로딩중...</span>
                </div>
                <p class="mt-2">추천 상품을 찾고 있습니다...</p>
            </div>

            <div id="results" style="display: none;">
                <h3 class="mb-3">추천 결과</h3>
                <div id="resultsList"></div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // 폼 제출 이벤트
    document.getElementById('profileForm').addEventListener('submit', function(e) {
        e.preventDefault();
        
        const formData = new FormData(this);
        const data = {
            age: parseInt(formData.get('age')),
            sex: formData.get('sex'),
            smoker_flag: parseInt(formData.get('smokerFlag')),
            monthly_budget: parseInt(formData.get('monthlyBudget')),
            top_n: parseInt(formData.get('topN'))
        };

        // 로딩 표시
        document.getElementById('loading').style.display = 'block';
        document.getElementById('results').style.display = 'none';

        // API 호출
        fetch('/cancer/profile-recommend/api', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(data)
        })
        .then(response => response.json())
        .then(data => {
            document.getElementById('loading').style.display = 'none';
            displayResults(data);
        })
        .catch(error => {
            document.getElementById('loading').style.display = 'none';
            console.error('Error:', error);
            alert('추천 요청 중 오류가 발생했습니다.');
        });
    });

    function displayResults(data) {
        const resultsDiv = document.getElementById('results');
        const resultsList = document.getElementById('resultsList');
        
        if (data.recommendations && data.recommendations.length > 0) {
            resultsList.innerHTML = '';
            
            data.recommendations.forEach((product, index) => {
                const card = document.createElement('div');
                card.className = 'result-card';
                card.innerHTML = `
                    <div class="mb-2">
                        <div class="company-name">${product.insuranceCompany}</div>
                        <div class="product-name">${product.productName}</div>
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="coverage-amount">${product.coverageAmount.toLocaleString()}원</div>
                            <div class="premium-info">
                                남성: ${product.malePremium ? product.malePremium.toLocaleString() + '원' : 'N/A'} | 
                                여성: ${product.femalePremium ? product.femalePremium.toLocaleString() + '원' : 'N/A'}
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="premium-info">
                                <strong>갱신형태:</strong> ${product.renewalCycle}<br>
                                <strong>판매채널:</strong> ${product.salesChannel}<br>
                                <strong>해약환급금:</strong> ${product.surrenderValue}
                            </div>
                        </div>
                    </div>
                `;
                resultsList.appendChild(card);
            });
            
            resultsDiv.style.display = 'block';
        } else {
            resultsList.innerHTML = '<div class="alert alert-warning">조건에 맞는 상품을 찾을 수 없습니다.</div>';
            resultsDiv.style.display = 'block';
        }
    }
</script>
</body>
</html>

