<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>거북이 보험 - 공지사항</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            /* 고정된 헤더 때문에 내용이 가려지는 것을 방지하기 위한 패딩 */
            padding-top: 80px; 
        }

        .custom-navbar {
            background-color: #fcf8f0;
            border-bottom: 1px solid #e0e0e0;
            padding: 1rem 1.5rem;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }
        .custom-navbar .navbar-brand {
            font-weight: 700;
            color: #17ec42;
            font-size: 1.8rem;
        }
        .custom-navbar .nav-link {
            color: #555;
            font-weight: 500;
            margin-right: 1.5rem;
        }
        .custom-navbar .nav-link:hover {
            color: #17ec42;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg custom-navbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="/main">🐢거북이 보험</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <b><a class="nav-link" href="/service_intro">서비스 소개</a></b>
                </li>
                <li class="nav-item">
                    <b><a class="nav-link" href="/user/my_insurance">내 보험찾기</a></b>
                </li>
                <li class="nav-item">
                    <b><a class="nav-link" href="/notice">공지사항</a></b>
                </li>
                <li class="nav-item d-flex align-items-center">
                    <div class="input-group">
                        <input type="search" class="form-control form-control-sm" placeholder="검색어 입력" aria-label="Search">
                        <button class="btn btn-outline-secondary btn-sm" type="button">
                            <i class="fas fa-search"></i>
                        </button>
                    </div>
                </li>
            </ul>
            <ul class="navbar-nav">
                {{#user}}
                    <li class="nav-item">
                        <b><span class="nav-link text-dark">안녕하세요. {{nickname}}님</span></b>
                    </li>
                    <li class="nav-item">
                        <form action="/logout" method="post" class="d-inline">
                            <b><button type="submit" class="btn btn-outline-dark">로그아웃</button></b>
                        </form>
                    </li>
                {{/user}}
                {{^user}}
                    <li class="nav-item">
                        <b><a class="nav-link text-dark" href="/user/login">로그인</a></b>
                    </li>
                    <li class="nav-item">
                        <b><a class="nav-link text-dark" href="/user/register">회원가입</a></b>
                    </li>
                {{/user}}
            </ul>
        </div>
    </div>
</nav>

<div class="container my-5">
    <div class="row">
        <div class="col-lg-10 mx-auto">

            <h2 class="mb-4 pb-2 border-bottom">공지사항</h2>

            {{#noticeList}}
                <div class="accordion" id="noticeAccordion">
                    {{#.}}
                        <div class="accordion-item">
                            <h2 class="accordion-header" id="heading-{{id}}">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-{{id}}">
                                    <strong>{{title}}</strong>
                                    <span class="ms-auto text-muted" style="font-size: 0.9rem;">{{createdAt}}</span>
                                </button>
                            </h2>
                            <div id="collapse-{{id}}" class="accordion-collapse collapse" data-bs-parent="#noticeAccordion">
                                <div class="accordion-body">
                                    {{{content}}}
                                </div>
                            </div>
                        </div>
                    {{/.}}
                </div>
            {{/noticeList}}

            {{^noticeList}}
                <div class="alert alert-secondary text-center" role="alert">
                    등록된 공지사항이 없습니다.
                </div>
            {{/noticeList}}

        </div>
    </div>
</div>

<footer class="bg-dark text-white text-center py-3">
    <p class="mb-0">©2025 Turtle Insurance. All Rights Reserved.</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>