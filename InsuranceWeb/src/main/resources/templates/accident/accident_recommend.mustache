<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>상해보험 추천</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            background-color: #f8f9fa;
            padding-top: 80px;
        }
        .custom-navbar {
            background-color: #1c2f48;
            border-bottom: 1px solid rgba(0,0,0,.25);
            padding: 8px 20px;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }
        .custom-navbar .navbar-brand {
            color: #d4b46b !important;
            font-weight: 900;
            font-size: 40px;
            margin-right: 30px;
        }
        .custom-navbar .nav-link {
            color: #d4b46b !important;
            font-weight: 800;
            font-size: 20px;
            margin-right: 36px;
        }
        .custom-navbar .nav-link:hover {
            color: #e5c57b !important;
        }
        .recommend-form {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(28,47,72,0.1);
            padding: 2.5rem;
            margin-bottom: 2rem;
        }
        .recommend-form h2 {
            color: #1c2f48;
            font-weight: 700;
            border-bottom: 3px solid #d4b46b;
            padding-bottom: 1rem;
            margin-bottom: 2rem;
        }
        .form-label {
            font-weight: 600;
            color: #1c2f48;
            margin-bottom: 0.5rem;
        }
        .form-control, .form-select {
            border: 2px solid #e9ecef;
            border-radius: 10px;
            padding: 0.75rem 1rem;
            font-size: 1rem;
            transition: all 0.3s ease;
        }
        .form-control:focus, .form-select:focus {
            border-color: #d4b46b;
            box-shadow: 0 0 0 0.2rem rgba(212,180,107,0.25);
        }
        .btn-primary {
            background-color: #1c2f48;
            border-color: #1c2f48;
            color: #d4b46b;
            border-radius: 10px;
            padding: 0.75rem 2rem;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #2a4a6f;
            border-color: #2a4a6f;
            color: #e5c57b;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(28,47,72,0.3);
        }
        .result-card {
            background: white;
            border-radius: 15px;
            padding: 1.5rem;
            margin: 1rem 0;
            box-shadow: 0 4px 15px rgba(28,47,72,0.1);
            border-left: 4px solid #d4b46b;
            transition: all 0.3s ease;
        }
        .result-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0,0,0,0.15);
        }
        .loading {
            display: none;
            text-align: center;
            padding: 2rem;
        }
        .badge {
            font-size: 0.9rem;
            padding: 0.5rem 1rem;
        }
        .btn-outline-primary {
            border-color: #1c2f48;
            color: #1c2f48;
        }
        .btn-outline-primary:hover {
            background-color: #1c2f48;
            color: #d4b46b;
            border-color: #1c2f48;
        }
        .section-title {
            color: #333;
            font-weight: 700;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        .product-name {
            color: #1c2f48;
            font-weight: 700;
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
        }
        .company-name {
            color: #666;
            font-weight: 600;
            margin-bottom: 1rem;
        }
        .coverage-info {
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 1rem;
            margin: 1rem 0;
        }
        .premium-info {
            background-color: #e8f5e8;
            border-radius: 10px;
            padding: 1rem;
            margin: 1rem 0;
        }
        .results-section {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            padding: 2rem;
            min-height: 600px;
        }
        .initial-message {
            text-align: center;
            color: #666;
            font-size: 1.1rem;
            margin-top: 3rem;
        }
        .loading-spinner {
            display: none;
            text-align: center;
            margin: 2rem 0;
        }
    </style>
</head>
<body>
    <!-- 네비게이션 바 -->
    <nav class="custom-navbar navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/main" style="color:#d4b46b; font-weight:900;">
                B
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/savings/recommend">연금보험</a>
                <a class="nav-link" href="/cancer/recommend">암보험</a>
                <a class="nav-link" href="/life/recommend">종신보험</a>
                <a class="nav-link active" href="/accident/recommend">상해보험</a>
                <a class="nav-link" href="/user/myPage">마이페이지</a>
                <a class="nav-link" href="/user/logout">로그아웃</a>
            </div>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <!-- 왼쪽 추천 설정 -->
            <div class="col-md-4">
                <div class="recommend-form">
                    <h3 class="section-title">
                        <i class="fas fa-shield-alt me-2"></i>상해보험 추천
                    </h3>
                    <p class="text-muted mb-4">나에게 맞는 상해보험을 찾아보세요</p>
                    
                    <form id="accidentForm">
                        <div class="mb-3">
                            <label class="form-label">나이</label>
                            <input type="number" class="form-control" id="age" name="age" min="1" max="100" placeholder="나이를 입력하세요" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">성별</label>
                            <select class="form-select" id="sex" name="sex" required>
                                <option value="">선택하세요</option>
                                <option value="male">남성</option>
                                <option value="female">여성</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">추천 상품 수</label>
                            <select class="form-select" id="topN" name="topN">
                                <option value="3">3개</option>
                                <option value="5" selected>5개</option>
                                <option value="10">10개</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">정렬 기준</label>
                            <select class="form-select" id="sortBy" name="sortBy">
                                <option value="default">기본</option>
                                <option value="premium">보험료</option>
                                <option value="coverage">보장금액</option>
                            </select>
                        </div>
                        
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="fas fa-search me-2"></i>상해보험 추천받기
                        </button>
                    </form>
                </div>
            </div>
            
            <!-- 오른쪽 추천 결과 -->
            <div class="col-md-8">
                <div class="results-section">
                    <div class="loading-spinner" id="loading">
                        <div class="spinner-border" role="status">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                        <p class="mt-3">추천 상품을 찾고 있습니다...</p>
                    </div>
                    
                    <div id="results">
                        <div class="initial-message">
                            <i class="fas fa-shield-alt fa-3x mb-3" style="color: #6c757d;"></i>
                            <h5 style="font-weight: 700; color: #333; margin-bottom: 0.5rem;">왼쪽 설정을 조정하고 "상해보험 찾기"를 클릭하세요!</h5>
                            <p style="color: #999; font-size: 1rem;">당신에게 최적화된 상해보험을 찾아드립니다.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('accidentForm').addEventListener('submit', async function(e) {
            e.preventDefault();

            const formData = {
                age: parseInt(document.getElementById('age').value),
                sex: document.getElementById('sex').value,
                topN: parseInt(document.getElementById('topN').value),
                sortBy: document.getElementById('sortBy').value
            };

            const loadingEl = document.querySelector('.loading-spinner');
            if (loadingEl) loadingEl.style.display = 'block';
            document.getElementById('results').innerHTML = '';

            try {
                const response = await fetch('/accident/recommend/api', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(formData)
                });

                const data = await response.json();
                displayResults(data);

            } catch (error) {
                console.error('Error:', error);
                document.getElementById('results').innerHTML =
                    '<div class="alert alert-danger">추천 서비스에 오류가 발생했습니다.</div>';
            } finally {
                const loadingEl = document.querySelector('.loading-spinner');
                if (loadingEl) loadingEl.style.display = 'none';
            }
        });

        function displayResults(data) {
            const resultsDiv = document.getElementById('results');

            if (!data.success || !data.recommendations || data.recommendations.length === 0) {
                resultsDiv.innerHTML = '<div class="alert alert-warning">추천할 상품이 없습니다.</div>';
                return;
            }


            data.recommendations.forEach((product, index) => {
                html += `
                    <div class="result-card">
                        <div class="row">
                            <div class="col-md-8">
                                <h5 class="product-name">${product.productName}</h5>
                                <p class="company-name"><i class="fas fa-building me-2"></i>${product.insuranceCompany}</p>

                                <div class="coverage-info">
                                    <h6><i class="fas fa-shield-alt me-2"></i>보장 정보</h6>
                                    <p class="mb-1"><strong>보장금액:</strong> ${product.coverageAmount.toLocaleString()}원</p>
                                    <p class="mb-0"><strong>갱신주기:</strong> ${product.renewalCycle}</p>
                                </div>

                                <div class="premium-info">
                                    <h6><i class="fas fa-won-sign me-2"></i>보험료 정보</h6>
                                    <p class="mb-1"><strong>남성 보험료:</strong> ${product.malePremium.toLocaleString()}원</p>
                                    <p class="mb-1"><strong>여성 보험료:</strong> ${product.femalePremium.toLocaleString()}원</p>
                                    <p class="mb-0"><strong>평균 보험료:</strong> ${product.avgPremium.toLocaleString()}원</p>
                                </div>
                            </div>
                            <div class="col-md-4 text-end">
                                <button class="btn btn-outline-primary btn-sm mb-2">상세보기</button>
                                <button class="btn btn-primary btn-sm" onclick="addToCart(${index})">
                                    관심 상품
                                </button>
                            </div>
                        </div>
                    </div>
                `;
            });

            resultsDiv.innerHTML = html;
        }

        // 관심 상품 추가 기능
        let currentProducts = [];
        
        function displayResults(data) {
            currentProducts = data.recommendations || []; // 상품 저장
            const resultsDiv = document.getElementById('results');

            if (!data.success || !data.recommendations || data.recommendations.length === 0) {
                resultsDiv.innerHTML = '<div class="alert alert-warning">추천할 상품이 없습니다.</div>';
                return;
            }

            let html = `

            `;

            data.recommendations.forEach((product, index) => {
                html += `
                    <div class="result-card">
                        <div class="row">
                            <div class="col-md-8">
                                <h5 class="product-name">${product.productName}</h5>
                                <p class="company-name"><i class="fas fa-building me-2"></i>${product.insuranceCompany}</p>

                                <div class="coverage-info">
                                    <h6><i class="fas fa-shield-alt me-2"></i>보장 정보</h6>
                                    <p class="mb-1"><strong>보장금액:</strong> ${product.coverageAmount.toLocaleString()}원</p>
                                    <p class="mb-0"><strong>갱신주기:</strong> ${product.renewalCycle}</p>
                                </div>

                                <div class="premium-info">
                                    <h6><i class="fas fa-won-sign me-2"></i>보험료 정보</h6>
                                    <p class="mb-1"><strong>남성 보험료:</strong> ${product.malePremium.toLocaleString()}원</p>
                                    <p class="mb-1"><strong>여성 보험료:</strong> ${product.femalePremium.toLocaleString()}원</p>
                                    <p class="mb-0"><strong>평균 보험료:</strong> ${product.avgPremium.toLocaleString()}원</p>
                                </div>
                            </div>

                        </div>
                    </div>
                `;
            });

            resultsDiv.innerHTML = html;
        }

        function addToCart(index) {
            const product = currentProducts[index];
            if (!product) {
                alert('상품 정보를 찾을 수 없습니다.');
                return;
            }

            const cartData = {
                insuranceType: "상해보험",
                insuranceCompany: product.insuranceCompany,
                productName: product.productName,
                coverageAmount: product.coverageAmount ? product.coverageAmount.toString() : '',
                malePremium: product.malePremium ? product.malePremium.toString() + '원' : '',
                femalePremium: product.femalePremium ? product.femalePremium.toString() + '원' : '',
                renewalCycle: product.renewalCycle || '',
                recommendationReason: product.specialNotes || ''
            };

            fetch('/cart/add', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(cartData)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert(data.message);
                } else {
                    if (data.message.includes('로그인')) {
                        if (confirm('로그인이 필요합니다. 로그인 페이지로 이동하시겠습니까?')) {
                            window.location.href = '/user/login';
                        }
                    } else {
                        alert(data.message);
                    }
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('관심 상품 추가 중 오류가 발생했습니다.');
            });
        }
    </script>
</body>
</html>
