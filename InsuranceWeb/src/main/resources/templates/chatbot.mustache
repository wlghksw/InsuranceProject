<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
    <title>보험 챗봇 - 거북이 보험</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Noto+Sans+KR', sans-serif;
            background-color: #f8f9fa;
            padding-top: 80px;
        }

        .custom-navbar {
            background-color: #fcf8f0;
            border-bottom: 1px solid #e0e0e0;
            padding: 1rem 1.5rem;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }

        .custom-navbar .navbar-brand {
            font-family: 'Noto Sans KR', sans-serif;
            font-weight: 700;
            color: #17ec42;
            font-size: 1.8rem;
        }

        .custom-navbar .nav-link {
            font-weight: 700;
            color: #555;
            margin-right: 1.5rem;
        }

        .custom-navbar .nav-link:hover {
            color: #17ec42;
        }

        .chat-container {
            max-width: 800px;
            margin: 2rem auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            overflow: hidden;
        }

        .chat-header {
            background: linear-gradient(135deg, #17ec42 0%, #14d63a 100%);
            color: white;
            padding: 1.5rem;
            text-align: center;
        }

        .chat-header h3 {
            margin: 0;
            font-weight: 700;
        }

        .chat-header p {
            margin: 0.5rem 0 0 0;
            font-size: 0.9rem;
            opacity: 0.9;
        }

        .chat-messages {
            height: 500px;
            overflow-y: auto;
            padding: 1.5rem;
            background: #f8f9fa;
        }

        .message {
            margin-bottom: 1rem;
            display: flex;
            align-items: flex-start;
        }

        .message.user {
            justify-content: flex-end;
        }

        .message-bubble {
            max-width: 70%;
            padding: 0.75rem 1rem;
            border-radius: 15px;
            word-wrap: break-word;
        }

        .message.bot .message-bubble {
            background-color: white;
            border: 1px solid #e0e0e0;
            color: #333;
        }

        .message.user .message-bubble {
            background-color: #17ec42;
            color: white;
        }

        .message-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 10px;
            flex-shrink: 0;
        }

        .message.bot .message-icon {
            background-color: #17ec42;
            color: white;
        }

        .message.user .message-icon {
            background-color: #6c757d;
            color: white;
        }

        .chat-input-container {
            padding: 1.5rem;
            background: white;
            border-top: 1px solid #e0e0e0;
        }

        .input-group input {
            border-radius: 25px 0 0 25px;
            border: 2px solid #e9ecef;
            padding: 0.75rem 1.5rem;
        }

        .input-group input:focus {
            border-color: #17ec42;
            box-shadow: none;
        }

        .input-group button {
            border-radius: 0 25px 25px 0;
            background-color: #17ec42;
            border-color: #17ec42;
            padding: 0.75rem 2rem;
            font-weight: 600;
        }

        .input-group button:hover {
            background-color: #14d63a;
            border-color: #14d63a;
        }

        .typing-indicator {
            display: none;
            padding: 0.5rem 1rem;
            background-color: white;
            border: 1px solid #e0e0e0;
            border-radius: 15px;
            max-width: 70px;
        }

        .typing-indicator span {
            height: 8px;
            width: 8px;
            background-color: #90949c;
            border-radius: 50%;
            display: inline-block;
            margin: 0 2px;
            animation: typing 1.4s infinite;
        }

        .typing-indicator span:nth-child(2) {
            animation-delay: 0.2s;
        }

        .typing-indicator span:nth-child(3) {
            animation-delay: 0.4s;
        }

        @keyframes typing {
            0%, 60%, 100% {
                transform: translateY(0);
            }
            30% {
                transform: translateY(-10px);
            }
        }

        .quick-questions {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            padding: 1rem 1.5rem;
            background: white;
            border-top: 1px solid #e0e0e0;
        }

        .quick-question-btn {
            padding: 0.5rem 1rem;
            border: 1px solid #17ec42;
            background: white;
            color: #17ec42;
            border-radius: 20px;
            font-size: 0.875rem;
            cursor: pointer;
            transition: all 0.3s;
        }

        .quick-question-btn:hover {
            background: #17ec42;
            color: white;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg custom-navbar">
        <div class="container-fluid">
            <a class="navbar-brand" href="/main">🐢 거북이 보험</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/main">홈</a></li>
                    <li class="nav-item"><a class="nav-link" href="/service_intro">서비스 소개</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/chatbot">챗봇</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Chat Container -->
    <div class="container">
        <div class="chat-container">
            <!-- Chat Header -->
            <div class="chat-header">
                <h3><i class="fas fa-robot me-2"></i>보험 AI 상담사</h3>
                <p>보험 용어와 관련된 질문을 자유롭게 해보세요!</p>
            </div>

            <!-- Chat Messages -->
            <div class="chat-messages" id="chatMessages">
                <div class="message bot">
                    <div class="message-icon">
                        <i class="fas fa-robot"></i>
                    </div>
                    <div class="message-bubble">
                        안녕하세요! 보험 AI 상담사입니다. 😊<br>
                        보험료, 보험금, 특약, 갱신 등 보험과 관련된 궁금한 점을 물어보세요!
                    </div>
                </div>
            </div>

            <!-- Quick Questions -->
            <div class="quick-questions">
                <button class="quick-question-btn" onclick="askQuestion('보험이란 무엇인가요?')">보험이란?</button>
                <button class="quick-question-btn" onclick="askQuestion('보험료와 보험금의 차이는?')">보험료 vs 보험금</button>
                <button class="quick-question-btn" onclick="askQuestion('특약이 뭔가요?')">특약이란?</button>
                <button class="quick-question-btn" onclick="askQuestion('갱신형과 비갱신형 차이')">갱신형 vs 비갱신형</button>
                <button class="quick-question-btn" onclick="askQuestion('실손보험이 뭔가요?')">실손보험</button>
            </div>

            <!-- Chat Input -->
            <div class="chat-input-container">
                <form id="chatForm">
                    <div class="input-group">
                        <input type="text" class="form-control" id="userInput" placeholder="질문을 입력하세요..." required>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-paper-plane me-2"></i>전송
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const chatMessages = document.getElementById('chatMessages');
        const chatForm = document.getElementById('chatForm');
        const userInput = document.getElementById('userInput');

        // 메시지를 채팅창에 추가
        function addMessage(message, isUser) {
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${isUser ? 'user' : 'bot'}`;
            
            if (isUser) {
                messageDiv.innerHTML = `
                    <div class="message-bubble">${message}</div>
                    <div class="message-icon">
                        <i class="fas fa-user"></i>
                    </div>
                `;
            } else {
                messageDiv.innerHTML = `
                    <div class="message-icon">
                        <i class="fas fa-robot"></i>
                    </div>
                    <div class="message-bubble">${message}</div>
                `;
            }
            
            chatMessages.appendChild(messageDiv);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        // 타이핑 인디케이터 표시/숨김
        function showTypingIndicator() {
            const typingDiv = document.createElement('div');
            typingDiv.className = 'message bot';
            typingDiv.id = 'typingIndicator';
            typingDiv.innerHTML = `
                <div class="message-icon">
                    <i class="fas fa-robot"></i>
                </div>
                <div class="typing-indicator" style="display: block;">
                    <span></span>
                    <span></span>
                    <span></span>
                </div>
            `;
            chatMessages.appendChild(typingDiv);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        }

        function hideTypingIndicator() {
            const typingIndicator = document.getElementById('typingIndicator');
            if (typingIndicator) {
                typingIndicator.remove();
            }
        }

        // 질문 전송
        async function askQuestion(question) {
            if (!question) return;
            
            // 사용자 메시지 추가
            addMessage(question, true);
            userInput.value = '';
            
            // 타이핑 인디케이터 표시
            showTypingIndicator();
            
            try {
                const response = await fetch('/chatbot/ask', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ question: question })
                });
                
                const data = await response.json();
                
                // 타이핑 인디케이터 숨김
                hideTypingIndicator();
                
                if (data.answer) {
                    addMessage(data.answer, false);
                } else {
                    addMessage('죄송합니다. 답변을 가져올 수 없습니다.', false);
                }
            } catch (error) {
                hideTypingIndicator();
                console.error('Error:', error);
                addMessage('오류가 발생했습니다. 잠시 후 다시 시도해주세요.', false);
            }
        }

        // 폼 제출 이벤트
        chatForm.addEventListener('submit', (e) => {
            e.preventDefault();
            const question = userInput.value.trim();
            if (question) {
                askQuestion(question);
            }
        });
    </script>
</body>
</html>
