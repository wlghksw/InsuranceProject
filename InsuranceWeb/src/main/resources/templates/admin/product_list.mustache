<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>보험 상품 목록 - 관리자</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            background-color: #f8f9fa;
            padding-top: 80px;
        }
        /* 네비게이션 바 - 메인과 동일 */
        .custom-navbar {
            background-color: #1c2f48;
            border-bottom: 1px solid rgba(0,0,0,.25);
            padding: 14px 20px;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }
        .custom-navbar .navbar-brand {
            color: #d4b46b !important;
            font-weight: 900;
            font-size: 32px;
        }
        .custom-navbar .nav-link {
            color: #d4b46b !important;
            font-weight: 700;
            font-size: 18px;
            margin-right: 24px;
        }
        .custom-navbar .nav-link:hover {
            color: #e5c57b !important;
        }
        .custom-navbar .btn-light {
            background-color: #d4b46b;
            border-color: #d4b46b;
            color: #1c2f48;
            font-weight: 600;
        }
        .custom-navbar .btn-light:hover {
            background-color: #e5c57b;
            border-color: #e5c57b;
        }
        .product-list {
            background: white;
            border-radius: 15px;
            padding: 2.5rem;
            box-shadow: 0 4px 20px rgba(28,47,72,0.1);
            margin-bottom: 3rem;
        }
        .product-list h4 {
            color: #1c2f48;
            font-weight: 700;
            border-bottom: 3px solid #d4b46b;
            padding-bottom: 1rem;
            margin-bottom: 2rem;
        }
        .btn-primary, .btn-success {
            background-color: #1c2f48;
            border-color: #1c2f48;
            color: #d4b46b;
            font-weight: 600;
        }
        .btn-primary:hover, .btn-success:hover {
            background-color: #2a4a6f;
            border-color: #2a4a6f;
            color: #e5c57b;
        }
        .btn-secondary {
            background-color: #6c757d;
            border-color: #6c757d;
        }
        .btn-warning {
            background-color: #ffc107;
            border-color: #ffc107;
            color: #1c2f48;
            font-weight: 600;
        }
        .btn-warning:hover {
            background-color: #ffca2c;
            border-color: #ffca2c;
            color: #1c2f48;
        }
        .btn-warning:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }
        .table thead {
            background-color: #1c2f48;
            color: #d4b46b;
        }
        .table thead th {
            border-color: #1c2f48;
            font-weight: 600;
        }
        .table-hover tbody tr:hover {
            background-color: rgba(212,180,107,0.1);
        }
        .badge {
            padding: 0.5rem 1rem;
            font-weight: 600;
        }
        .alert-success {
            background-color: rgba(212,180,107,0.1);
            border-color: #d4b46b;
            color: #1c2f48;
        }
        /* 탭 스타일 */
        .nav-tabs .nav-link {
            color: #1c2f48;
            font-weight: 600;
            border: 2px solid transparent;
        }
        .nav-tabs .nav-link:hover {
            color: #d4b46b;
            border-color: #e9ecef #e9ecef #dee2e6;
        }
        .nav-tabs .nav-link.active {
            color: #d4b46b;
            background-color: #1c2f48;
            border-color: #1c2f48 #1c2f48 #1c2f48;
        }
    </style>
</head>
<body>
    <!-- 상단 네비게이션 바 -->
    <nav class="navbar navbar-expand-lg custom-navbar">
        <div class="container-fluid">
            <a class="navbar-brand" href="/main" style="color:#d4b46b; font-weight:900;">
                B
            </a>
            <div class="d-flex align-items-center">
                <a href="/admin/dashboard" class="btn btn-light me-2">
                    <i class="fas fa-tachometer-alt me-2"></i>대시보드
                </a>
                <a href="/admin/products/new" class="btn btn-success me-2">
                    <i class="fas fa-plus-circle me-2"></i>상품 등록
                </a>
                <button class="btn btn-warning me-3" onclick="reloadFastAPIData()">
                    <i class="fas fa-sync-alt me-2"></i>데이터 새로고침
                </button>
                <span class="text-white me-3">관리자: <strong>{{loginAdminName}}</strong></span>
                <form action="/logout" method="post" class="d-inline">
                    <button type="submit" class="btn btn-secondary btn-sm">로그아웃</button>
                </form>
            </div>
        </div>
    </nav>

    <div class="container">
        {{#message}}
        <div class="alert alert-{{messageType}} alert-dismissible fade show" role="alert">
            {{message}}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{/message}}

        <div class="product-list">
            <h4 class="mb-4">
                <i class="fas fa-list me-2"></i>등록된 상품 
                <span class="badge bg-secondary">{{products.size}}개</span>
            </h4>

            {{#products.isEmpty}}
            <div class="alert alert-info text-center">
                <i class="fas fa-info-circle me-2"></i>
                등록된 상품이 없습니다.
            </div>
            {{/products.isEmpty}}

            {{^products.isEmpty}}
            <!-- 탭 네비게이션 -->
            <ul class="nav nav-tabs mb-3" id="productTabs" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link active" id="all-tab" data-bs-toggle="tab" data-bs-target="#all" type="button">
                        <i class="fas fa-th-large me-1"></i>전체
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="cancer-tab" data-bs-toggle="tab" data-bs-target="#cancer" type="button">
                        <i class="fas fa-ribbon me-1"></i>암보험
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="accident-tab" data-bs-toggle="tab" data-bs-target="#accident" type="button">
                        <i class="fas fa-first-aid me-1"></i>상해보험
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="savings-tab" data-bs-toggle="tab" data-bs-target="#savings" type="button">
                        <i class="fas fa-piggy-bank me-1"></i>저축성보험
                    </button>
                </li>
            </ul>

            <!-- 탭 컨텐츠 -->
            <div class="tab-content" id="productTabsContent">
                <!-- 전체 탭 -->
                <div class="tab-pane fade show active" id="all" role="tabpanel">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>ID</th>
                                    <th>유형</th>
                                    <th>보험회사</th>
                                    <th>상품명</th>
                                    <th>등록일</th>
                                    <th>관리</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#products}}
                                <tr id="product-row-{{id}}">
                                    <td>{{id}}</td>
                                    <td>
                                        <span class="badge bg-primary">{{productType}}</span>
                                    </td>
                                    <td>{{insuranceCompany}}</td>
                                    <td><strong>{{productName}}</strong></td>
                                    <td>{{createdAt}}</td>
                                    <td>
                                        <button class="btn btn-sm btn-danger" onclick="deleteProduct({{id}}, '{{productName}}')">
                                            <i class="fas fa-trash me-1"></i>삭제
                                        </button>
                                    </td>
                                </tr>
                                {{/products}}
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- 암보험 탭 -->
                <div class="tab-pane fade" id="cancer" role="tabpanel">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>ID</th>
                                    <th>보험회사</th>
                                    <th>상품명</th>
                                    <th>보장금액</th>
                                    <th>남성 보험료</th>
                                    <th>여성 보험료</th>
                                    <th>갱신주기</th>
                                    <th>등록일</th>
                                    <th>관리</th>
                                </tr>
                            </thead>
                            <tbody id="cancer-tbody">
                                <!-- JavaScript로 동적 생성 -->
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- 상해보험 탭 -->
                <div class="tab-pane fade" id="accident" role="tabpanel">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>ID</th>
                                    <th>보험회사</th>
                                    <th>상품명</th>
                                    <th>보장금액</th>
                                    <th>남성 보험료</th>
                                    <th>여성 보험료</th>
                                    <th>갱신주기</th>
                                    <th>등록일</th>
                                    <th>관리</th>
                                </tr>
                            </thead>
                            <tbody id="accident-tbody">
                                <!-- JavaScript로 동적 생성 -->
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- 저축성보험 탭 -->
                <div class="tab-pane fade" id="savings" role="tabpanel">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>ID</th>
                                    <th>보험회사</th>
                                    <th>상품명</th>
                                    <th>월 납입금</th>
                                    <th>최저보증이율</th>
                                    <th>현재공시이율</th>
                                    <th>납입방법</th>
                                    <th>등록일</th>
                                    <th>관리</th>
                                </tr>
                            </thead>
                            <tbody id="savings-tbody">
                                <!-- JavaScript로 동적 생성 -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            {{/products.isEmpty}}
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // 상품 데이터를 JavaScript로 가져오기
        const allProducts = [
            {{#products}}
            {
                id: {{id}},
                productType: "{{productType}}",
                insuranceCompany: "{{insuranceCompany}}",
                productName: "{{productName}}",
                coverageAmount: "{{coverageAmount}}",
                malePremium: "{{malePremium}}",
                femalePremium: "{{femalePremium}}",
                renewalCycle: "{{renewalCycle}}",
                monthlyPremium: "{{monthlyPremium}}",
                guaranteedRate: "{{guaranteedRate}}",
                currentRate: "{{currentRate}}",
                paymentMethod: "{{paymentMethod}}",
                createdAt: "{{createdAt}}"
            }{{^last}},{{/last}}
            {{/products}}
        ];

        // 페이지 로드 시 각 탭에 상품 표시
        document.addEventListener('DOMContentLoaded', function() {
            renderProductsByType('암보험', 'cancer-tbody');
            renderProductsByType('상해보험', 'accident-tbody');
            renderProductsByType('저축성보험', 'savings-tbody');
        });

        function renderProductsByType(type, tbodyId) {
            const tbody = document.getElementById(tbodyId);
            const products = allProducts.filter(p => p.productType === type);
            
            if (products.length === 0) {
                tbody.innerHTML = '<tr><td colspan="9" class="text-center text-muted">등록된 상품이 없습니다.</td></tr>';
                return;
            }

            tbody.innerHTML = '';
            products.forEach(product => {
                const tr = document.createElement('tr');
                tr.id = `product-row-${type}-${product.id}`;
                
                if (type === '저축성보험') {
                    tr.innerHTML = `
                        <td>${product.id}</td>
                        <td>${product.insuranceCompany}</td>
                        <td><strong>${product.productName}</strong></td>
                        <td>${product.monthlyPremium || '-'}</td>
                        <td>${product.guaranteedRate || '-'}</td>
                        <td>${product.currentRate || '-'}</td>
                        <td>${product.paymentMethod || '-'}</td>
                        <td>${product.createdAt}</td>
                        <td>
                            <button class="btn btn-sm btn-danger" onclick="deleteProduct(${product.id}, '${product.productName}')">
                                <i class="fas fa-trash me-1"></i>삭제
                            </button>
                        </td>
                    `;
                } else {
                    // 암보험, 상해보험
                    tr.innerHTML = `
                        <td>${product.id}</td>
                        <td>${product.insuranceCompany}</td>
                        <td><strong>${product.productName}</strong></td>
                        <td>${product.coverageAmount || '-'}</td>
                        <td>${product.malePremium || '-'}</td>
                        <td>${product.femalePremium || '-'}</td>
                        <td>${product.renewalCycle || '-'}</td>
                        <td>${product.createdAt}</td>
                        <td>
                            <button class="btn btn-sm btn-danger" onclick="deleteProduct(${product.id}, '${product.productName}')">
                                <i class="fas fa-trash me-1"></i>삭제
                            </button>
                        </td>
                    `;
                }
                tbody.appendChild(tr);
            });
        }

        function deleteProduct(productId, productName) {
            if (!confirm(`"${productName}" 상품을 삭제하시겠습니까?\n\n삭제 후 FastAPI 서버를 재시작해야 추천에 반영됩니다.`)) {
                return;
            }

            fetch(`/admin/products/${productId}`, {
                method: 'DELETE',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => {
                if (response.ok) {
                    alert('상품이 삭제되었습니다.');
                    // 행 제거 애니메이션
                    const row = document.getElementById(`product-row-${productId}`);
                    if (row) {
                        row.style.backgroundColor = '#ffcccc';
                        setTimeout(() => {
                            row.remove();
                            // 상품이 모두 삭제되면 새로고침
                            const tbody = document.querySelector('tbody');
                            if (tbody && tbody.children.length === 0) {
                                location.reload();
                            }
                        }, 300);
                    }
                } else if (response.status === 404) {
                    alert('상품을 찾을 수 없습니다.');
                } else {
                    alert('상품 삭제 중 오류가 발생했습니다.');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('서버 오류가 발생했습니다.');
            });
        }

        // FastAPI 데이터 재로딩 함수
        function reloadFastAPIData() {
            if (!confirm('FastAPI 서버의 데이터를 새로고침하시겠습니까?\n\n새로 등록한 상품이 추천에 반영됩니다.')) {
                return;
            }

            // 로딩 표시
            const btn = event.target.closest('button');
            const originalHTML = btn.innerHTML;
            btn.disabled = true;
            btn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i>새로고침 중...';

            fetch('http://localhost:8002/admin/reload-data', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => response.json())
            .then(data => {
                btn.disabled = false;
                btn.innerHTML = originalHTML;
                
                if (data.success) {
                    alert('✅ 데이터 새로고침 완료!\n\n재로딩된 엔진:\n- ' + data.reloaded_engines.join('\n- '));
                } else {
                    alert('⚠️ 데이터 새로고침 실패: ' + (data.message || '알 수 없는 오류'));
                }
            })
            .catch(error => {
                btn.disabled = false;
                btn.innerHTML = originalHTML;
                console.error('Error:', error);
                alert('❌ FastAPI 서버와 통신 중 오류가 발생했습니다.\n\nFastAPI 서버가 실행 중인지 확인하세요.');
            });
        }
    </script>
</body>
</html>

