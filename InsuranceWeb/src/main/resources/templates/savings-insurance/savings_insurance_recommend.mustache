<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
    <title>저축성보험 추천 - 거북이 보험</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Noto Sans KR', sans-serif;
            background-color: #f8f9fa;
            padding-top: 80px;
        }
        .custom-navbar {
            background-color: #1c2f48;
            border-bottom: 1px solid rgba(0,0,0,.25);
            padding: 8px 20px;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }
        .custom-navbar .navbar-brand {
            color: #d4b46b !important;
            font-weight: 900;
            font-size: 40px;
            margin-right: 30px;
        }
        .custom-navbar .nav-link {
            color: #d4b46b !important;
            font-weight: 800;
            font-size: 20px;
            margin-right: 36px;
        }
        .custom-navbar .nav-link:hover {
            color: #e5c57b !important;
        }
        .recommend-form {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            padding: 2rem;
            margin-bottom: 2rem;
        }
        .form-label {
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
        }
        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 10px;
            padding: 0.75rem 1rem;
            font-size: 1rem;
        }
        .form-control:focus, .form-select:focus {
            border-color: #d4b46b;
            box-shadow: 0 0 0 0.2rem rgba(212,180,107,0.25);
        }
        .btn-primary {
            background-color: #1c2f48;
            border-color: #1c2f48;
            color: #d4b46b;
            border-radius: 10px;
            padding: 0.75rem 2rem;
            font-weight: 600;
            font-size: 1.1rem;
        }
        .btn-primary:hover {
            background-color: #2a4a6f;
            border-color: #2a4a6f;
            color: #e5c57b;
        }
        .result-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(28,47,72,0.1);
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            border-left: 5px solid #d4b46b;
        }
        .loading {
            text-align: center;
            padding: 2rem;
            color: #666;
        }
        .badge {
            font-size: 0.9rem;
            padding: 0.5rem 1rem;
        }
        .btn-outline-primary {
            border-color: #1c2f48;
            color: #1c2f48;
            border-radius: 8px;
        }
        .btn-outline-primary:hover {
            background-color: #1c2f48;
            color: #d4b46b;
            border-color: #1c2f48;
        }
        .section-title {
            color: #333;
            font-weight: 700;
            margin-bottom: 1rem;
            border-bottom: 2px solid #d4b46b;
            padding-bottom: 0.5rem;
        }
        .product-name {
            font-size: 1.3rem;
            font-weight: 700;
            color: #333;
            margin-bottom: 0.5rem;
        }
        .company-name {
            color: #666;
            font-size: 1rem;
            margin-bottom: 1rem;
        }
        .coverage-info {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 1rem;
        }
        .premium-info {
            background-color: #e8f5e8;
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 1rem;
        }
        .detail-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.5rem 0;
            border-bottom: 1px solid #eee;
        }
        .detail-label {
            font-weight: 600;
            color: #555;
        }
        .detail-value {
            color: #333;
            font-weight: 500;
        }
        .recommendation-reason {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 8px;
            padding: 1rem;
            margin-top: 1rem;
        }
        .reason-header {
            color: #856404;
            font-weight: 600;
        }
        .reason-content {
            color: #856404;
            margin-top: 0.5rem;
        }
        .results-section {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            padding: 2rem;
            min-height: 600px;
        }
        .initial-message {
            text-align: center;
            color: #666;
            font-size: 1.1rem;
            margin-top: 3rem;
        }
        .loading-spinner {
            display: none;
            text-align: center;
            margin: 2rem 0;
        }
        .section-title {
            font-weight: 700;
            color: #333;
            margin-bottom: 1rem;
            font-size: 1.3rem;
            border-bottom: 3px solid #d4b46b;
            padding-bottom: 0.5rem;
        }
    </style>
</head>
<body>
    <!-- 네비게이션 바 -->
    <nav class="custom-navbar navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/main" style="color:#d4b46b; font-weight:900;">
                B
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/savings/recommend">연금보험</a>
                <a class="nav-link" href="/cancer/recommend">암보험</a>
                <a class="nav-link" href="/life/recommend">종신보험</a>
                <a class="nav-link" href="/accident/recommend">상해보험</a>
                <a class="nav-link active" href="/savings-insurance/recommend">저축성보험</a>
                <a class="nav-link" href="/user/myPage">마이페이지</a>
                <a class="nav-link" href="/user/logout">로그아웃</a>
            </div>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <!-- 왼쪽 추천 설정 -->
            <div class="col-md-4">
                <div class="recommend-form">
                    <h3 class="section-title">
                        <i class="fas fa-piggy-bank me-2"></i>저축성보험 추천
                    </h3>
                    <p class="text-muted mb-4">나에게 맞는 저축성보험을 찾아보세요</p>
                    
                    <form id="recommendForm">
                        <div class="mb-3">
                            <label for="monthlyBudget" class="form-label">월 예산</label>
                            <select class="form-select" id="monthlyBudget" name="monthlyBudget" required>
                                <option value="100000">10만원</option>
                                <option value="200000">20만원</option>
                                <option value="300000" selected>30만원</option>
                                <option value="500000">50만원</option>
                                <option value="700000">70만원</option>
                                <option value="1000000">100만원</option>
                                <option value="1500000">150만원</option>
                                <option value="2000000">200만원</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="purpose" class="form-label">저축 목적</label>
                            <select class="form-select" id="purpose" name="purpose" required>
                                <option value="단기저축">단기저축 (1-3년)</option>
                                <option value="중기저축">중기저축 (3-7년)</option>
                                <option value="장기저축">장기저축 (7년 이상)</option>
                                <option value="교육자금">교육자금</option>
                                <option value="주택자금">주택자금</option>
                                <option value="노후자금">노후자금</option>
                                <option value="세제혜택">세제혜택</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label for="minGuaranteedRate" class="form-label">최소 보장 금리 (%)</label>
                            <input type="number" class="form-control" id="minGuaranteedRate" name="minGuaranteedRate" min="0" max="10" step="0.1" placeholder="예: 1.0">
                        </div>
                        <div class="mb-3">
                            <label for="topN" class="form-label">추천 상품 수</label>
                            <select class="form-select" id="topN" name="topN">
                                <option value="3">3개</option>
                                <option value="5" selected>5개</option>
                                <option value="10">10개</option>
                            </select>
                        </div>
                        
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="fas fa-search me-2"></i>저축성보험 추천받기
                        </button>
                    </form>
                </div>
            </div>
            
            <!-- 오른쪽 추천 결과 -->
            <div class="col-md-8">
                <div class="results-section">
                    <div class="loading-spinner" id="loading-spinner">
                        <div class="spinner-border" role="status">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                        <p class="mt-3">추천 상품을 찾고 있습니다...</p>
                    </div>
                    
                    <div id="results">
                        <div class="initial-message">
                            <i class="fas fa-shield-alt fa-3x mb-3" style="color: #6c757d;"></i>
                            <h5 style="font-weight: 700; color: #333; margin-bottom: 0.5rem;">왼쪽 설정을 조정하고 "저축성보험 찾기"를 클릭하세요!</h5>
                            <p style="color: #999; font-size: 1rem;">당신에게 최적화된 저축성보험을 찾아드립니다.</p>
                        </div>
                    </div>
                    
                    <div id="recommendations"></div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('recommendForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const formData = new FormData(this);
            const purpose = formData.get('purpose');
            
            // purpose 값 검증
            const validPurposes = ['단기저축', '중기저축', '장기저축', '교육자금', '주택자금', '노후자금', '세제혜택'];
            if (!validPurposes.includes(purpose)) {
                alert('올바른 저축 목적을 선택해주세요.');
                return;
            }
            
            const data = {
                age: 35, // 고정값 (백엔드에서 사용하지 않음)
                monthlyBudget: parseInt(formData.get('monthlyBudget')),
                purpose: purpose,
                preferUniversal: null,
                minGuaranteedRate: formData.get('minGuaranteedRate') ? parseFloat(formData.get('minGuaranteedRate')) : null,
                topN: parseInt(formData.get('topN'))
            };
            
            // 디버깅: 전송할 데이터 확인
            console.log('전송할 데이터:', data);
            console.log('선택된 purpose:', purpose);
            
            // 로딩 표시
            const loadingEl = document.querySelector('.loading-spinner');
            if (loadingEl) loadingEl.style.display = 'block';
            document.getElementById('results').innerHTML = '';
            
            try {
                const response = await fetch('/savings-insurance/recommend/api', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(data)
                });
                
                const result = await response.json();
                
                if (result.success) {
                    displayResults(result.recommendations);
                } else {
                    alert('추천 중 오류가 발생했습니다: ' + result.message);
                }
            } catch (error) {
                console.error('Error:', error);
                alert('추천중 오류발생했데');
            } finally {
                const loadingEl = document.querySelector('.loading-spinner');
                if (loadingEl) loadingEl.style.display = 'none';
            }
        });
        
        function displayResults(recommendations) {
            const container = document.getElementById('recommendations');
            container.innerHTML = '';
            
            if (recommendations.length === 0) {
                container.innerHTML = '<div class="alert alert-warning">추천할 상품이 없습니다.</div>';
                // 결과 표시 (results는 항상 보이므로 제거)
                return;
            }
            
            recommendations.forEach((rec, index) => {
                const card = document.createElement('div');
                card.className = 'result-card';
                card.innerHTML = `
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div>
                            <div class="product-name">${rec.product_name || '정보없음'}</div>
                            <div class="company-name">${rec.company || '정보없음'}</div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="coverage-info">
                                <h6 class="fw-bold mb-3">보장 정보</h6>
                                <div class="detail-item">
                                    <div class="detail-label">최소 보장 금리</div>
                                    <div class="detail-value">${rec.guaranteed_rate || '정보없음'}</div>
                                </div>
                                <div class="detail-item">
                                    <div class="detail-label">현재 공시이율</div>
                                    <div class="detail-value">${rec.current_rate || '정보없음'}</div>
                                </div>
                                <div class="detail-item">
                                    <div class="detail-label">보험기간</div>
                                    <div class="detail-value">${rec.term || '정보없음'}</div>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="premium-info">
                                <h6 class="fw-bold mb-3">납입 정보</h6>
                                <div class="detail-item">
                                    <div class="detail-label">월 납입금</div>
                                    <div class="detail-value">${rec.monthly_premium || '정보없음'}</div>
                                </div>
                                <div class="detail-item">
                                    <div class="detail-label">해약환급금</div>
                                    <div class="detail-value">${rec.surrender_value || '정보없음'}</div>
                                </div>
                                <div class="detail-item">
                                    <div class="detail-label">납입방법</div>
                                    <div class="detail-value">${rec.payment_method || '정보없음'}</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- 추천 이유 -->
                    <div class="recommendation-reason mt-3">
                        <div class="reason-header">
                            <strong>추천 이유:</strong>
                        </div>
                        <div class="reason-content mt-1">
                            ${rec.recommendation_reason || '추천 이유 없음'}
                        </div>
                    </div>
                    
                    <!-- 관심 상품 추가 버튼 -->
                    <div class="text-end mt-3">
                        <button class="btn btn-primary" onclick="addToCart(${index})">
                            관심 상품
                        </button>
                    </div>
                `;
                container.appendChild(card);
            });
            
            // 결과 표시 (results는 항상 보이므로 제거)
        }

        // 관심 상품 추가 기능
        let currentSavingsProducts = [];

        function displayRecommendations(recommendations) {
            currentSavingsProducts = recommendations; // 상품 저장
            const container = document.getElementById('resultsContainer');
            container.innerHTML = '';

            recommendations.forEach((rec, index) => {
                const card = document.createElement('div');
                card.className = 'result-card';
                card.innerHTML = `
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div>
                            <div class="product-name">${rec.product_name || '정보없음'}</div>
                            <div class="company-name">${rec.company || '정보없음'}</div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <div class="coverage-info">
                                <h6 class="fw-bold mb-3">보장 정보</h6>
                                <div class="detail-item">
                                    <div class="detail-label">최소 보장 금리</div>
                                    <div class="detail-value">${rec.guaranteed_rate || '정보없음'}</div>
                                </div>
                                <div class="detail-item">
                                    <div class="detail-label">현재 공시이율</div>
                                    <div class="detail-value">${rec.current_rate || '정보없음'}</div>
                                </div>
                                <div class="detail-item">
                                    <div class="detail-label">보험기간</div>
                                    <div class="detail-value">${rec.term || '정보없음'}</div>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="premium-info">
                                <h6 class="fw-bold mb-3">납입 정보</h6>
                                <div class="detail-item">
                                    <div class="detail-label">월 납입금</div>
                                    <div class="detail-value">${rec.monthly_premium || '정보없음'}</div>
                                </div>
                                <div class="detail-item">
                                    <div class="detail-label">해약환급금</div>
                                    <div class="detail-value">${rec.surrender_value || '정보없음'}</div>
                                </div>
                                <div class="detail-item">
                                    <div class="detail-label">납입방법</div>
                                    <div class="detail-value">${rec.payment_method || '정보없음'}</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- 추천 이유 -->
                    <div class="recommendation-reason mt-3">
                        <div class="reason-header">
                            <strong>추천 이유:</strong>
                        </div>
                        <div class="reason-content mt-1">
                            ${rec.recommendation_reason || '추천 이유 없음'}
                        </div>
                    </div>
                    
                    <!-- 관심 상품 추가 버튼 -->
                    <div class="text-end mt-3">
                        <button class="btn btn-primary" onclick="addToCart(${index})">
                            관심 상품
                        </button>
                    </div>
                `;
                container.appendChild(card);
            });
            
            // 결과 표시 (results는 항상 보이므로 제거)
        }

        function addToCart(index) {
            const product = currentSavingsProducts[index];
            if (!product) {
                alert('상품 정보를 찾을 수 없습니다.');
                return;
            }

            const cartData = {
                insuranceType: "저축성보험",
                insuranceCompany: product.company,
                productName: product.product_name,
                monthlyPremium: product.monthly_premium || '',
                guaranteedRate: product.guaranteed_rate || '',
                currentRate: product.current_rate || '',
                term: product.term || '',
                paymentMethod: product.payment_method || '',
                recommendationReason: product.recommendation_reason || ''
            };

            fetch('/cart/add', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(cartData)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert(data.message);
                } else {
                    if (data.message.includes('로그인')) {
                        if (confirm('로그인이 필요합니다. 로그인 페이지로 이동하시겠습니까?')) {
                            window.location.href = '/user/login';
                        }
                    } else {
                        alert(data.message);
                    }
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('관심 상품 추가 중 오류가 발생했습니다.');
            });
        }
    </script>
</body>
</html>
